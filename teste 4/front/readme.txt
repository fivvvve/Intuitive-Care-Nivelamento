To run the frontend on your machine do the following:

1. Make sure you have node installed in your enviremont

2. Download all the dependencies with:
npm install

3. Run with:
npm run dev

Note:
You have to create a .env file in the root of the directory and add the following to the file in case you want to search for the data:
VITE_BACKEND_URL="YourBackendUrl"